export default function Square({ num }) {
  return <div className="square">{num}</div>;
}

// Square({ num })
// this essentially means "Go and get me the num property from the object that is passed to Square"
